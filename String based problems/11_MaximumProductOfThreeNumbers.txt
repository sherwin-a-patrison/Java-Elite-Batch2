Maximum Product of Three Numbers:

class Solution {
    public int maximumProduct(int[] nums) {
        Arrays.sort(nums);
        int n=nums.length-1;
        int pr1=nums[n]*nums[n-1]*nums[n-2];
        int pr2=nums[0]*nums[1]*nums[n];
        return pr1>pr2?pr1:pr2;
    }
}