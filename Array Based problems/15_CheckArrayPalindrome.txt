Check array palindrome:

class Solution {
    public static boolean isPerfect(int[] arr) {
        int n=arr.length;
	    for(int i=0;i<n;i++){
	        if(arr[i]!=arr[(n-1)-i]) 
			return false;
	    }
	    return true;
	}
}